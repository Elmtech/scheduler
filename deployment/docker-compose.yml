ssh:
  build: .
  dockerfile: ssh-dockerfile
  container_name: scheduler-ssh
  ports:
   - "21212:22"
  volumes:
   - /home/webmaster

gunicorn:
  build: .
  dockerfile: python-dockerfile
  container_name: scheduler-python
  volumes_from:
   - ssh

nginx:
  build: .
  dockerfile: nginx-dockerfile
  container_name: scheduler-nginx
  ports:
   - "80:80"
  volumes_from:
   - ssh
  links:
   - gunicorn:app
